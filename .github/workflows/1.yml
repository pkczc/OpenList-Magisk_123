# This is the reverted content of the workflow file.
# Original content prior to commit b1cc5ea03ac55cb150c55a56ed3ad597eafb42da

name: Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm test

# Additional steps can be added here based on the previous workflow.